{
  "name": "heating-control-aws-lambda",
  "version": "1.2.0",
  "description": "AWS Lambda function to process events from Alexa",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "rm lambda.zip; npm install; zip -rq lambda.zip *"
  },
  "keywords": [
    "alexa",
    "voice",
    "lambda",
    "automation",
    "heating",
    "garage"
  ],
  "author": "Shane Weaver",
  "license": "Apache-2.0",
  "dependencies": {
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {}
}
